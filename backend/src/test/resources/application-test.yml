spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///erip_test?TC_TMPFS=/testtmpfs:rw
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers:localhost:9092}

erip:
  jwt:
    secret: test-secret-key-for-unit-and-integration-tests-only-not-for-prod
  ml-service:
    base-url: http://localhost:8001
  llm:
    provider: mock
